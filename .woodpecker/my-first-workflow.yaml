when:
  - event: push
    branch: main

steps:
  build:
    image: docker/compose
    commands:
      - docker-compose build
  
  deploy:
    image: docker/compose
    commands:
      - docker-compose up -d